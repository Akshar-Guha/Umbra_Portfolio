const Footer = () => {
  return (
    <footer className="w-screen h-screen bg-black text-white flex items-center justify-center">
      {/* Terminal-style prompt and Matrix rain effect will go here */}
      <p>Footer Section (Terminal Prompt)</p>
    </footer>
  );
};

export default Footer; 